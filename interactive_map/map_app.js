/**
 * Координаты в системе viewBox карты 0 0 1200 675.
 * Каждой области задайте: x, y (смещение) и scale (масштаб).
 * В редакторе (клавиша E) можно мышкой двигать и колёсиком менять масштаб.
 */
const FPEDVESGA_PATH =
  "M20.1984 92L16.6984 89V81.5L20.1984 77C20.5318 76 21.4984 73.8 22.6984 73C24.1984 72 28.1984 70 30.1984 69C32.1984 68 35.1984 67 41.6984 62.5C45.1984 61 44.6984 59.5 62.1984 53C64.1984 50 65.6984 53 72.6984 53C76.6984 53 75.1984 53 75.6984 50C76.0984 47.6 73.8651 44.6667 72.6984 43.5C72.0318 42.5 70.2984 40.1 68.6984 38.5C66.6984 36.5 66.6984 34.5 65.6984 33C64.6984 31.5 58.6984 33 56.1984 33C53.6984 33 48.6984 28 46.6984 26C45.0984 24.4 42.6984 23.3333 41.6984 23C38.8651 22.3333 32.5984 20.8 30.1984 20C27.7984 19.2 26.1984 17.6667 25.6984 17C24.1984 16.1667 22.5984 13.8 28.1984 11C33.7984 8.2 31.8651 8.5 30.1984 9L25.6984 6.5V0.5H317.698L315.198 3.5L312.698 6.5L305.198 9L302.198 11H291.698L287.698 9H274.198C267.798 9 266.198 10.3333 266.198 11H257.198C252.365 12.1667 241.998 14.9 239.198 16.5C236.398 18.1 231.365 17.1667 229.198 16.5L215.698 17H200.698H191.698L183.198 23L175.698 26L172.698 28L180.698 30C183.365 28.6667 189.798 25.4 194.198 23C198.598 20.6 198.698 24 198.198 26L200.698 30L202.698 33H198.198C196.198 37 199.032 41.6667 200.698 43.5C203.032 45 208.498 48.4 211.698 50C215.698 52 213.198 53 215.698 56C218.198 59 221.698 56 227.698 56C230.198 55 229.198 53 232.198 53C235.198 53 236.198 54 239.198 53C241.598 52.2 242.532 50.6667 242.698 50C243.365 49.1667 245.098 47.2 246.698 46C248.698 44.5 248.198 43.5 252.198 40.5C256.198 37.5 255.198 38.5 259.198 36.5C263.198 34.5 265.698 34.5 271.198 33C275.598 31.8 276.365 30.5 276.198 30L287.698 28C294.532 27.3333 308.598 26 310.198 26C311.798 26 314.198 27.3333 315.198 28C317.365 29.3333 321.998 31.6 323.198 30C324.398 28.4 328.032 26.6667 329.698 26L337.198 23C338.865 23 342.698 23.6 344.698 26C346.698 28.4 349.198 27 350.198 26L355.198 23H362.198C364.198 23 365.032 25 365.198 26L371.698 23H378.698L381.698 26L385.198 30C387.198 29.6667 391.598 28.4 393.198 26C395.198 23 396.198 24 398.698 23C401.198 22 402.698 26 409.198 30C410.798 33.2 418.198 31.3333 421.698 30C424.532 29.3333 430.698 27.6 432.698 26C434.698 24.4 439.865 23.3333 442.198 23L451.198 19L463.198 16H469.698L476.198 13.5L482.198 16H489.198L492.698 19H499.198H506.198C507.698 18 511.398 15.5 514.198 13.5C516.998 11.5 516.698 10.6667 516.198 10.5H521.198C522.032 9.66667 524.598 8.5 528.198 10.5C531.798 12.5 532.032 13.3333 531.698 13.5C533.198 14 538.198 13.6 546.198 8C549.398 6 549.532 7.16667 549.198 8L560.698 1H827.698L825.698 5.5C824.532 7.66667 822.098 12 821.698 12C821.298 12 820.198 14.3333 819.698 15.5H812.698C810.698 15.5 803.698 9.5 802.198 9.5H800.198L795.198 12C793.598 12.8 792.198 14.6667 791.698 15.5H787.198C785.698 15.5 784.198 17.5 783.198 17.5C782.198 17.5 779.198 21 778.198 22C777.398 22.8 775.865 23 775.198 23C773.365 22.6667 769.498 21.5 768.698 19.5C767.898 17.5 766.698 17.3333 766.198 17.5C765.532 18.1667 764.298 20 764.698 22C765.198 24.5 764.198 24.5 763.198 26C762.398 27.2 761.865 27.5 761.698 27.5H755.698C754.698 27.5 753.698 28 752.698 27.5C751.698 27 750.698 27 750.698 26V23V19.5C750.698 18.7 749.698 17.8333 749.198 17.5H747.198L744.698 19.5C743.532 20.3333 740.998 22.2 740.198 23C739.398 23.8 738.532 24.3333 738.198 24.5L734.698 30L729.698 40.5C729.198 42.6667 727.498 47.4 724.698 49C721.198 51 719.698 50.5 716.698 51.5C713.698 52.5 707.698 53.5 706.698 53.5H696.198H689.698C686.198 53.5 684.698 55 683.698 55.5C682.698 56 679.698 57 677.198 57.5C675.198 57.9 675.032 59.6667 675.198 60.5V64L677.198 67C677.998 68.2 677.532 71.5 677.198 73C676.198 73.5 673.898 74.8 672.698 76C671.198 77.5 669.198 77.5 666.198 79C663.198 80.5 663.198 79 654.698 79C651.698 79 651.198 81.5 649.698 81.5C648.198 81.5 645.198 82 642.198 81.5C639.798 81.1 638.865 81.6667 638.698 82C636.532 82.8333 631.998 84.5 631.198 84.5H629.698L627.698 85.5L628.698 88.5C629.532 88.8333 631.798 89.9 634.198 91.5C636.598 93.1 636.198 92.1667 635.698 91.5C636.365 92 637.898 93 638.698 93C639.498 93 640.698 94 641.198 94.5V96L645.198 97H649.698C658.498 84.6 682.365 81.8333 693.198 82C701.698 69.5 722.698 74 729.698 76C736.698 78 745.698 70.5 748.198 70.5C750.698 70.5 757.698 77 760.198 79C762.698 81 764.198 91.5 764.198 94.5C764.198 96.9 769.198 97.8333 771.698 98C773.698 102 776.898 109.9 773.698 109.5C769.698 109 755.698 123 755.198 125C754.798 126.6 753.032 127.333 752.198 127.5C750.365 126.667 746.498 125 745.698 125H742.698L739.698 127.5L737.198 130L734.698 131.5V136L732.698 140H728.198L724.698 141.5C714.698 147.9 710.532 146.5 709.698 145C703.298 132.6 693.698 140.833 689.698 146.5C681.298 147.7 666.865 146 660.698 145C649.098 155.8 633.865 154.167 627.698 152C616.898 138.4 602.865 139.667 597.198 142C595.198 145.333 588.398 150.6 577.198 145C563.198 138 547.198 151 542.198 153.5C537.198 156 507.198 149.5 503.198 138.5C499.998 129.7 490.198 133.5 485.698 136.5C471.698 150.9 461.198 145.5 457.698 141C461.298 127.4 451.198 127 445.698 128.5C440.498 141.7 429.532 145 424.698 145C419.532 145 408.398 146.2 405.198 151C401.998 155.8 395.865 154.667 393.198 153.5C391.365 152.833 387.598 151.1 387.198 149.5C386.798 147.9 388.698 146.833 389.698 146.5H394.698H402.698C406.032 145.167 411.198 142.2 405.198 141C397.698 139.5 360.698 145 355.198 153.5C350.798 160.3 343.032 160 339.698 159C323.198 144.5 305.198 146 291.698 145C278.198 144 262.198 123 257.198 122.5C252.198 122 234.198 119 221.698 137C211.698 151.4 195.532 152.333 188.698 151C180.698 148.5 163.998 144 161.198 146C158.398 148 150.032 150.167 146.198 151C127.198 151 134.698 152.5 123.698 156C114.898 158.8 107.032 157.167 104.198 156C82.9984 151.2 74.3651 155.667 72.6984 158.5C65.1984 165.667 48.4984 177.6 41.6984 168C33.1984 156 20.1984 153 10.6984 154C1.19843 155 -1.30158 142.5 1.69842 141.5C4.09842 140.7 6.03175 135.833 6.69842 133.5C6.36509 131.667 5.89842 126.3 6.69842 119.5C7.49842 112.7 11.0318 110.333 12.6984 110L16.6984 106.5C18.1984 104.667 21.1984 100.2 21.1984 97C21.1984 93.8 20.5318 92.3333 20.1984 92Z";

const SEVAS_PATH =
  "M73.8967 11.25L61.8967 5.75C57.0967 3.55 52.23 8.5 50.3967 11.25C51.23 13.4167 50.0967 17.75 38.8967 17.75C24.8967 17.75 31.3967 25.25 21.3967 27.75C13.3967 29.75 9.39668 41.9167 8.39668 47.75C-0.803323 51.35 -0.103323 66.5833 1.39668 73.75C12.9967 75.35 26.23 89.75 31.3967 96.75C49.3967 96.75 44.8967 96.75 56.8967 100.25C66.4967 103.05 73.8967 101.417 76.3967 100.25C134.397 84.25 101.897 91.25 128.397 96.75C149.597 101.15 171.23 94.9167 179.397 91.25C201.397 100.05 213.23 89.9167 216.397 83.75C221.563 86.25 233.997 90.25 242.397 86.25C250.797 82.25 285.23 95.5833 301.397 102.75C320.563 100.917 357.097 91.05 349.897 66.25C342.697 41.45 322.23 25.5833 312.897 20.75C294.397 -3.25 240.897 0.75 219.397 0.75C202.197 0.75 183.23 12.0833 175.897 17.75C174.897 13.75 170.697 6.85 161.897 11.25C150.897 16.75 139.397 11.25 133.897 11.25C129.497 11.25 118.73 15.5833 113.897 17.75C113.563 15.5833 112.497 11.25 110.897 11.25C109.297 11.25 104.23 17.5833 101.897 20.75L87.8967 16.25L73.8967 11.25Z";

const NALES_PATH =
  "M20.7036 34.2961C32.7036 31.0961 41.7036 19.6294 44.7036 14.2961C46.8703 12.1294 52.3036 8.49607 56.7036 11.2961C61.1036 14.0961 67.2036 14.4627 69.7036 14.2961C74.537 18.1294 85.8036 24.1961 92.2036 17.7961C98.6036 11.3961 107.87 12.7961 111.704 14.2961L125.704 7.79607C133.204 12.7961 149.204 14.2961 161.204 14.2961C170.804 14.2961 190.204 9.96273 198.704 7.79607C213.904 14.5961 226.704 7.29606 231.204 2.79606C237.204 5.1294 250.404 8.39606 255.204 2.79606C261.204 -4.20394 283.704 6.29606 304.704 14.2961C321.504 20.6961 336.37 16.9627 341.704 14.2961L347.204 17.7961C351.704 17.7961 362.204 19.3961 368.204 25.7961C374.204 32.1961 370.704 45.1294 368.204 50.7961C361.537 53.4627 349.204 61.0961 353.204 70.2961C357.204 79.4961 347.204 82.7961 341.704 83.2961C349.304 87.2961 344.87 93.2961 341.704 95.7961C328.504 109.796 313.87 108.963 308.204 106.796C303.704 108.463 293.804 112.396 290.204 114.796C286.604 117.196 277.704 119.129 273.704 119.796C264.504 108.596 257.537 111.796 255.204 114.796L241.204 111.796L231.204 119.796H217.204C201.204 119.796 191.87 126.129 189.204 129.296C174.204 129.296 172.204 127.296 149.204 119.796C126.204 112.296 133.704 119.796 111.704 123.796C102.204 109.796 87.7036 110.796 79.2036 114.796C72.4036 117.996 65.7036 116.129 63.2036 114.796C54.2036 105.963 36.5036 91.0961 37.7036 102.296C38.9036 113.496 31.537 118.629 27.7036 119.796C23.7036 118.129 14.0036 115.796 7.20363 119.796C0.703633 117.796 -1.29637 110.296 2.20363 106.796C5.00363 103.996 6.70363 93.2961 7.20363 88.2961L2.20363 78.2961L7.20363 70.2961C23.2036 59.8961 22.8703 52.9627 20.7036 50.7961C15.7036 46.6294 8.70363 37.4961 20.7036 34.2961Z";

const TEF_PATH =
  "M127.107 17.0798C134.941 16.2465 154.807 17.5798 171.607 29.5798C175.941 27.5798 187.107 23.5798 197.107 23.5798C209.607 23.5798 212.107 29.5798 212.107 35.5798C212.107 41.5798 222.607 47.5798 223.607 35.5798C224.607 23.5798 216.607 42.0798 234.107 29.5798C248.107 19.5798 259.941 17.0798 264.107 17.0798C268.107 22.9131 282.107 31.0798 306.107 17.0798C330.107 3.07981 339.107 3.57981 340.607 5.57981C342.107 1.74648 346.007 -3.62019 349.607 5.57981C354.107 17.0798 347.107 17.0798 361.107 17.0798C372.307 17.0798 374.774 13.0798 374.607 11.0798C386.441 15.0798 408.107 26.3798 400.107 39.5798C392.107 52.7798 423.774 70.4131 440.607 77.5798C448.607 73.2465 465.107 62.8798 467.107 56.0798C473.907 62.0798 478.941 65.9131 480.607 67.0798C478.607 63.9131 475.807 55.7798 480.607 48.5798C486.607 39.5798 506.607 41.0798 511.607 48.5798C515.607 54.5798 530.274 42.4131 537.107 35.5798C540.107 31.2465 548.507 25.1798 558.107 35.5798C566.107 23.5798 567.107 23.0798 567.107 17.0798C567.107 12.2798 579.107 19.4131 585.107 23.5798C591.441 21.2465 605.107 15.4798 609.107 11.0798C614.107 5.57981 629.107 -6.4202 644.107 14.5798C656.107 31.3798 668.441 38.2465 673.107 39.5798C682.441 52.2465 700.307 81.4798 697.107 97.0798C702.607 100.746 713.107 112.38 711.107 129.58C703.774 136.246 688.007 151.48 683.607 159.08C678.107 168.58 679.607 152.08 673.107 176.08C667.907 195.28 658.274 184.08 654.107 176.08C649.107 173.413 635.607 171.58 621.607 185.58C607.607 199.58 586.441 196.746 577.607 193.58C565.607 189.413 540.707 180.08 537.107 176.08C533.507 172.08 524.274 174.413 520.107 176.08C511.274 179.246 492.307 185.58 487.107 185.58C480.607 185.58 481.607 171.08 467.107 193.58C463.941 190.913 457.107 185.58 455.107 185.58C452.607 185.58 439.107 199.08 430.107 200.08C422.907 200.88 407.107 209.08 400.107 213.08C383.441 216.413 347.507 221.48 337.107 215.08C326.707 208.68 317.774 215.08 314.607 219.08C308.441 217.58 292.907 211.68 280.107 200.08C264.107 185.58 256.107 202.58 234.107 176.08C225.274 174.746 200.407 176.38 171.607 193.58C161.607 195.746 139.707 200.08 132.107 200.08C128.441 206.58 118.207 216.98 106.607 206.58H99.1072C88.6072 202.246 64.5072 193.58 52.1072 193.58C39.7072 193.58 32.2739 197.913 30.1072 200.08C28.4405 192.08 22.0072 176.08 9.6072 176.08C-2.7928 176.08 0.107197 158.413 3.1072 149.58C7.44053 145.413 18.9072 136.88 30.1072 136.08C44.1072 135.08 42.1072 136.08 70.6072 136.08C76.7739 127.413 85.4072 108.78 70.6072 103.58C55.8072 98.3798 61.7739 94.0798 66.6072 92.5798C61.7739 91.0798 52.1072 85.9798 52.1072 77.5798L40.1072 67.0798C39.2739 63.4131 40.5072 56.0798 52.1072 56.0798C63.7072 56.0798 66.6072 38.4131 66.6072 29.5798C66.7739 22.2465 70.3072 8.2798 83.1072 11.0798C95.9072 13.8798 99.1072 8.57981 99.1072 5.57981C102.941 7.57981 111.107 13.9798 113.107 23.5798L127.107 17.0798Z";

const WEST_STATES_PATH =
  "M143.355 16.6185L124.855 8.11849L119.855 5.11849H112.355C108.355 2.28516 100.755 -2.18151 102.355 2.61849C103.955 7.41849 95.0212 17.6185 90.3545 22.1185C81.5545 20.1185 74.6878 22.9518 72.3545 24.6185H63.3545H55.8545V32.1185L49.3545 40.6185L38.3545 45.6185C40.5212 48.1185 42.8545 53.9185 34.8545 57.1185C26.8545 60.3185 26.1879 66.1185 26.8545 68.6185C30.6879 71.9518 36.0545 79.9185 26.8545 85.1185C17.6545 90.3185 23.0212 97.9518 26.8545 101.118V107.118L20.8545 113.618V118.118V127.618L12.8545 135.118L9.85451 142.618L4.35451 159.118C6.18784 164.952 8.75451 177.719 4.35451 182.118C-1.14549 187.618 0.85451 190.118 0.85451 195.618C0.85451 200.018 6.85451 206.118 9.85451 208.618L12.8545 221.118L20.8545 224.118C26.6878 220.785 38.3545 215.518 38.3545 221.118C38.3545 226.719 50.0212 236.452 55.8545 240.618C58.6878 239.285 64.1545 239.118 63.3545 249.118C62.5545 259.118 69.0212 261.952 72.3545 262.118C86.3545 241.618 80.3545 256.118 88.3545 249.118C96.3545 242.118 96.3545 234.618 97.3545 230.618C98.1545 227.419 103.355 229.285 105.855 230.618L110.355 234.618L119.855 240.618H130.855L137.855 246.118H146.355L153.355 256.118L158.855 252.118L167.855 246.118L174.355 240.618C176.688 239.618 181.755 237.419 183.355 236.618C184.955 235.818 190.021 232.285 192.355 230.618L199.355 224.118L211.855 221.118L219.355 214.118H223.355L231.855 221.118V227.618L235.855 234.618L249.855 227.618V221.118L258.355 214.118H265.355L268.855 208.618L273.355 201.618H283.355L288.855 195.618H297.355V182.118C297.355 179.318 294.688 171.952 293.355 168.618L288.855 162.618L276.855 155.618L273.355 151.618L268.855 142.618L273.355 135.118L276.855 127.618L283.355 122.118H293.355H300.355L304.855 118.118L309.855 113.618C313.688 117.118 324.655 122.918 337.855 118.118C354.355 112.118 345.855 95.1185 345.355 91.1185C344.955 87.9185 337.855 85.7852 334.355 85.1185C338.355 70.7185 327.355 68.1185 321.355 68.6185V62.1185C318.688 61.9518 312.655 59.9185 309.855 53.1185C307.055 46.3185 314.021 41.9518 317.855 40.6185H325.855C327.021 37.9518 330.355 31.0185 334.355 24.6185C338.355 22.6185 336.021 18.4518 334.355 16.6185V8.11849H329.355V2.61849H317.855C315.855 2.61849 314.021 6.28516 313.355 8.11849L304.855 13.1185L300.355 8.11849H297.355L293.355 2.61849H283.355L276.855 8.11849V13.1185H273.355L268.855 8.11849H265.355H261.355L249.855 2.61849H235.855H231.855H219.355H211.855L204.855 8.11849H199.355H192.355H188.355L181.355 13.1185C179.688 12.9518 174.655 11.7185 167.855 8.11849C161.055 4.51849 159.021 6.61849 158.855 8.11849L153.355 16.6185H143.355Z";

const REGIONS = [
  {
    id: "fpedvesga",
    title: "Фпедвесга",
    d: FPEDVESGA_PATH,
    x: 206,
    y: 25,
    scale: 1,
  },
  {
    id: "sevas",
    title: "Севас",
    d: SEVAS_PATH,
    x: 486,
    y: 461,
    scale: 0.85,
  },
  {
    id: "nales",
    title: "Налес",
    d: NALES_PATH,
    x: 468,
    y: 534,
    scale: 0.87,
  },
  {
    id: "tef",
    title: "Теф",
    d: TEF_PATH,
    x: 413,
    y: 244,
    scale: 0.87,
  },
  {
    id: "west_states",
    title: "Западные государства",
    d: WEST_STATES_PATH,
    x: 175,
    y: 257,
    scale: 0.88,
  },
];

const SAMPLE_LINES = [
  "Ancient trade routes once linked these borders before the last frost.",
  "Envoys report uneasy quiet along the frontier; the harvest was poor.",
  "Local chronicles mention a sealed vault beneath the old market square.",
  "The coastward cliffs still bear scars from the storm of ’fourteen.",
  "Rumor speaks of a wandering cartographer who mapped what should not be seen.",
  "Tax ledgers from the capital show a sudden spike in river tolls.",
  "Herds have been moved inland; wolves—or worse—were seen at dusk.",
  "A minor noble house disputes succession; the crown has not intervened.",
  "Pilgrims avoid the eastern pass until the meltwater recedes.",
  "Smithies work double shifts; something is being forged in secret.",
];

function randomBody() {
  const n = 2 + Math.floor(Math.random() * 2);
  const lines = [];
  const pool = SAMPLE_LINES.slice();
  for (let i = 0; i < n && pool.length; i++) {
    const j = Math.floor(Math.random() * pool.length);
    lines.push(pool.splice(j, 1)[0]);
  }
  return lines.join(" ");
}

function buildTransform(r) {
  const x = r.x ?? 0;
  const y = r.y ?? 0;
  const s = r.scale ?? 1;
  return `translate(${x} ${y}) scale(${s})`;
}

function applyTransform(poly, r) {
  poly.setAttribute("transform", buildTransform(r));
}

function renderRegions(svg) {
  REGIONS.forEach((r) => {
    const tagName = r.d ? "path" : "polygon";
    const poly = document.createElementNS("http://www.w3.org/2000/svg", tagName);

    if (r.d) {
      poly.setAttribute("d", r.d);
    } else {
      poly.setAttribute("points", r.points);
    }
    applyTransform(poly, r);

    poly.setAttribute("class", "region");
    poly.setAttribute("data-id", r.id);
    poly.setAttribute("tabindex", "0");
    poly.setAttribute("role", "button");
    poly.setAttribute("aria-label", r.title);
    svg.appendChild(poly);
  });
}

/* ---------- Editor mode ---------- */

function createEditorUI() {
  const panel = document.createElement("div");
  panel.id = "editor-panel";
  panel.innerHTML = `
    <div class="ed-head">
      <strong>Editor</strong>
      <span class="ed-hint">E — вкл/выкл · клик — выбрать · drag — двигать · колесо — масштаб · ←↑→↓ — точно · +/− — масштаб · C — копировать</span>
    </div>
    <div class="ed-body">
      <label>Область:
        <select id="ed-region"></select>
      </label>
      <div class="ed-row">
        <label>x <input id="ed-x" type="number" step="1"></label>
        <label>y <input id="ed-y" type="number" step="1"></label>
        <label>scale <input id="ed-s" type="number" step="0.05" min="0.05"></label>
      </div>
      <pre id="ed-out"></pre>
    </div>
  `;
  document.body.appendChild(panel);
  return panel;
}

function initEditor(svg, regions) {
  const panel = createEditorUI();
  const select = panel.querySelector("#ed-region");
  const inX = panel.querySelector("#ed-x");
  const inY = panel.querySelector("#ed-y");
  const inS = panel.querySelector("#ed-s");
  const out = panel.querySelector("#ed-out");

  REGIONS.forEach((r) => {
    const opt = document.createElement("option");
    opt.value = r.id;
    opt.textContent = r.title;
    select.appendChild(opt);
  });

  let editing = false;
  let currentId = REGIONS[0]?.id ?? null;

  function getMeta(id) {
    return REGIONS.find((r) => r.id === id);
  }
  function getPoly(id) {
    return svg.querySelector(`.region[data-id="${id}"]`);
  }

  function refreshUI() {
    const meta = getMeta(currentId);
    if (!meta) return;
    select.value = meta.id;
    inX.value = Math.round(meta.x);
    inY.value = Math.round(meta.y);
    inS.value = Number(meta.scale.toFixed(3));
    out.textContent =
      `{ id: "${meta.id}", x: ${Math.round(meta.x)}, y: ${Math.round(meta.y)}, scale: ${Number(meta.scale.toFixed(3))} }`;
  }

  function update() {
    const meta = getMeta(currentId);
    const poly = getPoly(currentId);
    if (!meta || !poly) return;
    applyTransform(poly, meta);
    refreshUI();
    regions.forEach((p) =>
      p.classList.toggle("is-editing", p.getAttribute("data-id") === currentId)
    );
  }

  function setEditing(v) {
    editing = v;
    document.body.classList.toggle("editor-on", editing);
    panel.classList.toggle("is-open", editing);
    if (editing) update();
    else regions.forEach((p) => p.classList.remove("is-editing"));
  }

  document.addEventListener("keydown", (e) => {
    if (e.target && ["INPUT", "TEXTAREA", "SELECT"].includes(e.target.tagName))
      return;
    if (e.key === "e" || e.key === "E" || e.key === "у" || e.key === "У") {
      setEditing(!editing);
      return;
    }
    if (!editing) return;
    const meta = getMeta(currentId);
    if (!meta) return;
    const step = e.shiftKey ? 10 : 1;
    if (e.key === "ArrowLeft") {
      meta.x -= step;
      update();
      e.preventDefault();
    } else if (e.key === "ArrowRight") {
      meta.x += step;
      update();
      e.preventDefault();
    } else if (e.key === "ArrowUp") {
      meta.y -= step;
      update();
      e.preventDefault();
    } else if (e.key === "ArrowDown") {
      meta.y += step;
      update();
      e.preventDefault();
    } else if (e.key === "+" || e.key === "=") {
      meta.scale = Math.max(0.05, meta.scale * 1.05);
      update();
      e.preventDefault();
    } else if (e.key === "-" || e.key === "_") {
      meta.scale = Math.max(0.05, meta.scale / 1.05);
      update();
      e.preventDefault();
    } else if (e.key === "c" || e.key === "C" || e.key === "с" || e.key === "С") {
      navigator.clipboard?.writeText(out.textContent).catch(() => {});
    }
  });

  select.addEventListener("change", () => {
    currentId = select.value;
    update();
  });
  inX.addEventListener("input", () => {
    const m = getMeta(currentId);
    if (m) {
      m.x = Number(inX.value) || 0;
      update();
    }
  });
  inY.addEventListener("input", () => {
    const m = getMeta(currentId);
    if (m) {
      m.y = Number(inY.value) || 0;
      update();
    }
  });
  inS.addEventListener("input", () => {
    const m = getMeta(currentId);
    if (m) {
      m.scale = Math.max(0.05, Number(inS.value) || 0.05);
      update();
    }
  });

  function clientToViewBox(evt) {
    const pt = svg.createSVGPoint();
    pt.x = evt.clientX;
    pt.y = evt.clientY;
    const ctm = svg.getScreenCTM();
    if (!ctm) return { x: 0, y: 0 };
    const p = pt.matrixTransform(ctm.inverse());
    return { x: p.x, y: p.y };
  }

  let dragState = null;

  regions.forEach((poly) => {
    poly.addEventListener("pointerdown", (e) => {
      if (!editing) return;
      const id = poly.getAttribute("data-id");
      currentId = id;
      const meta = getMeta(id);
      const p = clientToViewBox(e);
      dragState = {
        id,
        startX: p.x,
        startY: p.y,
        origX: meta.x,
        origY: meta.y,
      };
      poly.setPointerCapture(e.pointerId);
      e.preventDefault();
      e.stopPropagation();
      update();
    });
    poly.addEventListener("pointermove", (e) => {
      if (!editing || !dragState || dragState.id !== poly.getAttribute("data-id"))
        return;
      const p = clientToViewBox(e);
      const meta = getMeta(dragState.id);
      meta.x = dragState.origX + (p.x - dragState.startX);
      meta.y = dragState.origY + (p.y - dragState.startY);
      update();
    });
    const endDrag = (e) => {
      if (dragState && dragState.id === poly.getAttribute("data-id")) {
        dragState = null;
        try {
          poly.releasePointerCapture(e.pointerId);
        } catch (_) {}
      }
    };
    poly.addEventListener("pointerup", endDrag);
    poly.addEventListener("pointercancel", endDrag);

    poly.addEventListener(
      "wheel",
      (e) => {
        if (!editing) return;
        e.preventDefault();
        const id = poly.getAttribute("data-id");
        currentId = id;
        const meta = getMeta(id);
        const factor = e.deltaY < 0 ? 1.08 : 1 / 1.08;
        meta.scale = Math.max(0.05, meta.scale * factor);
        update();
      },
      { passive: false }
    );
  });

  refreshUI();
}

/* ---------- Audio FX (procedural, Web Audio API) ---------- */

const AudioFX = (() => {
  let ctx = null;
  let master = null;
  const audioEls = {};

  const SAMPLE_URLS = {
    decaying: "decaying-sonic-boom.mp3",
    stress: "stress-transition-sound.mp3",
    swing: "slow-swing.mp3",
  };

  const SAMPLE_DOM_IDS = {
    decaying: "snd-decaying",
    stress: "snd-stress",
    swing: "snd-swing",
  };

  function ensure() {
    if (!ctx) {
      try {
        const Ctor = window.AudioContext || window.webkitAudioContext;
        if (!Ctor) return null;
        ctx = new Ctor();
        master = ctx.createGain();
        master.gain.value = 0.7;
        master.connect(ctx.destination);
      } catch (_) {
        return null;
      }
    }
    if (ctx.state === "suspended") {
      ctx.resume().catch(() => {});
    }
    return ctx;
  }

  function getAudioEl(name) {
    if (audioEls[name]) return audioEls[name];
    const domId = SAMPLE_DOM_IDS[name];
    let el = domId ? document.getElementById(domId) : null;
    if (!el) {
      const url = SAMPLE_URLS[name];
      if (!url) return null;
      try {
        el = new Audio(url);
        el.preload = "auto";
      } catch (_) {
        return null;
      }
    }
    audioEls[name] = el;
    return el;
  }

  function preloadAudio() {
    Object.keys(SAMPLE_URLS).forEach((name) => {
      const el = getAudioEl(name);
      if (el && typeof el.load === "function") {
        try { el.load(); } catch (_) {}
      }
    });
  }

  function primeAudio() {
    Object.keys(SAMPLE_URLS).forEach((name) => {
      const el = getAudioEl(name);
      if (!el) return;
      try {
        el.muted = true;
        const p = el.play();
        const reset = () => {
          try {
            el.pause();
            el.currentTime = 0;
            el.muted = false;
          } catch (_) {}
        };
        if (p && typeof p.then === "function") {
          p.then(reset).catch(() => {
            el.muted = false;
          });
        } else {
          reset();
        }
      } catch (_) {}
    });
  }

  function animateVolume(el, from, to, durSec, onDone) {
    const start = performance.now();
    const total = Math.max(1, durSec * 1000);
    el.volume = Math.max(0, Math.min(1, from));
    const tick = () => {
      if (!el) return;
      const k = Math.min(1, (performance.now() - start) / total);
      el.volume = Math.max(0, Math.min(1, from + (to - from) * k));
      if (k < 1) requestAnimationFrame(tick);
      else if (onDone) onDone();
    };
    requestAnimationFrame(tick);
  }

  function playAudioEl(name, opts = {}) {
    const el = getAudioEl(name);
    if (!el) return;
    const { volume = 0.65, fadeIn = 0.1, fadeOut = 0.6 } = opts;
    try {
      el.pause();
      el.currentTime = 0;
    } catch (_) {}
    el.muted = false;
    el.volume = 0;
    const p = el.play();
    if (p && typeof p.catch === "function") p.catch(() => {});

    animateVolume(el, 0, volume, fadeIn);

    const scheduleFadeOut = () => {
      const dur = isFinite(el.duration) && el.duration > 0 ? el.duration : 2;
      const fadeOutStart = Math.max(fadeIn + 0.05, dur - fadeOut);
      window.setTimeout(() => {
        const startVol = el.volume;
        animateVolume(el, startVol, 0, fadeOut, () => {
          try {
            el.pause();
            el.currentTime = 0;
          } catch (_) {}
        });
      }, fadeOutStart * 1000);
    };

    if (isFinite(el.duration) && el.duration > 0) {
      scheduleFadeOut();
    } else {
      const onMeta = () => {
        el.removeEventListener("loadedmetadata", onMeta);
        scheduleFadeOut();
      };
      el.addEventListener("loadedmetadata", onMeta);
    }
  }

  function playDecayingSonicBoom() {
    playAudioEl("decaying", { volume: 0.62, fadeIn: 0.05, fadeOut: 1.6 });
  }

  function playStressTransition() {
    playAudioEl("stress", { volume: 0.75, fadeIn: 0.08, fadeOut: 1.3 });
  }

  function playSlowSwing() {
    playAudioEl("swing", { volume: 0.6, fadeIn: 0.12, fadeOut: 1.1 });
  }

  function unlock() {
    ensure();
    primeAudio();
  }

  ["pointerdown", "keydown", "touchstart", "mousedown"].forEach((ev) => {
    window.addEventListener(ev, unlock, { capture: true, passive: true });
  });

  function noiseBuffer(dur) {
    const c = ensure();
    if (!c) return null;
    const frames = Math.max(1, Math.floor(c.sampleRate * dur));
    const buf = c.createBuffer(1, frames, c.sampleRate);
    const data = buf.getChannelData(0);
    for (let i = 0; i < frames; i++) data[i] = Math.random() * 2 - 1;
    return buf;
  }

  function playWhoosh() {
    const c = ensure();
    if (!c) return;
    const now = c.currentTime;
    const dur = 2.0;

    const src = c.createBufferSource();
    src.buffer = noiseBuffer(dur);

    const filter = c.createBiquadFilter();
    filter.type = "bandpass";
    filter.Q.value = 1.1;
    filter.frequency.setValueAtTime(180, now);
    filter.frequency.exponentialRampToValueAtTime(3800, now + 0.55);
    filter.frequency.exponentialRampToValueAtTime(420, now + dur);

    const gain = c.createGain();
    gain.gain.setValueAtTime(0.0001, now);
    gain.gain.linearRampToValueAtTime(0.55, now + 0.35);
    gain.gain.linearRampToValueAtTime(0.35, now + 1.0);
    gain.gain.exponentialRampToValueAtTime(0.0005, now + dur);

    src.connect(filter).connect(gain).connect(master);
    src.start(now);
    src.stop(now + dur + 0.05);

    const sub = c.createOscillator();
    sub.type = "sine";
    sub.frequency.setValueAtTime(70, now);
    sub.frequency.exponentialRampToValueAtTime(40, now + dur);
    const sg = c.createGain();
    sg.gain.setValueAtTime(0.0001, now);
    sg.gain.linearRampToValueAtTime(0.22, now + 0.4);
    sg.gain.exponentialRampToValueAtTime(0.0005, now + dur);
    sub.connect(sg).connect(master);
    sub.start(now);
    sub.stop(now + dur + 0.05);
  }

  function playClick() {
    const c = ensure();
    if (!c) return;
    const now = c.currentTime;
    const osc = c.createOscillator();
    osc.type = "triangle";
    osc.frequency.setValueAtTime(1600, now);
    osc.frequency.exponentialRampToValueAtTime(520, now + 0.07);
    const g = c.createGain();
    g.gain.setValueAtTime(0.0001, now);
    g.gain.exponentialRampToValueAtTime(0.09, now + 0.005);
    g.gain.exponentialRampToValueAtTime(0.0001, now + 0.1);
    osc.connect(g).connect(master);
    osc.start(now);
    osc.stop(now + 0.12);
  }

  function playFrost() {
    const c = ensure();
    if (!c) return;
    const now = c.currentTime;
    const dur = 1.9;

    const src = c.createBufferSource();
    src.buffer = noiseBuffer(dur);
    const hp = c.createBiquadFilter();
    hp.type = "highpass";
    hp.frequency.value = 3800;
    const ng = c.createGain();
    ng.gain.setValueAtTime(0.0001, now);
    ng.gain.linearRampToValueAtTime(0.22, now + 0.15);
    ng.gain.exponentialRampToValueAtTime(0.0005, now + dur);
    src.connect(hp).connect(ng).connect(master);
    src.start(now);
    src.stop(now + dur);

    [1760, 2637, 3520, 4186].forEach((f, i) => {
      const start = now + i * 0.09;
      const o = c.createOscillator();
      o.type = "sine";
      o.frequency.setValueAtTime(f, start);
      const g = c.createGain();
      g.gain.setValueAtTime(0.0001, start);
      g.gain.linearRampToValueAtTime(0.14, start + 0.02);
      g.gain.exponentialRampToValueAtTime(0.0005, start + 1.3);
      o.connect(g).connect(master);
      o.start(start);
      o.stop(start + 1.35);
    });
  }

  function playTef() {
    const c = ensure();
    if (!c) return;
    const now = c.currentTime;
    const dur = 3.4;

    // Warm rain under a dense canopy — softened by a low shelf of leaves.
    const rain = c.createBufferSource();
    rain.buffer = noiseBuffer(dur);
    const rainBp = c.createBiquadFilter();
    rainBp.type = "bandpass";
    rainBp.frequency.setValueAtTime(1600, now);
    rainBp.frequency.linearRampToValueAtTime(1300, now + dur);
    rainBp.Q.value = 0.7;
    const rainLp = c.createBiquadFilter();
    rainLp.type = "lowpass";
    rainLp.frequency.value = 2400;
    const rainG = c.createGain();
    rainG.gain.setValueAtTime(0.0001, now);
    rainG.gain.linearRampToValueAtTime(0.18, now + 0.55);
    rainG.gain.linearRampToValueAtTime(0.14, now + dur - 0.9);
    rainG.gain.exponentialRampToValueAtTime(0.0005, now + dur);
    rain.connect(rainBp).connect(rainLp).connect(rainG).connect(master);
    rain.start(now);
    rain.stop(now + dur);

    // Scattered drops on wide leaves — bright, short clicks.
    for (let i = 0; i < 18; i++) {
      const t = now + 0.25 + Math.random() * (dur - 1.0);
      const tail = Math.min(1, (dur - (t - now)) / 0.8, (t - now) / 0.4);
      const ds = c.createBufferSource();
      ds.buffer = noiseBuffer(0.09);
      const df = c.createBiquadFilter();
      df.type = "bandpass";
      df.frequency.value = 2200 + Math.random() * 2400;
      df.Q.value = 5.5;
      const dg = c.createGain();
      dg.gain.setValueAtTime(0.0001, t);
      dg.gain.linearRampToValueAtTime(0.09 * tail, t + 0.004);
      dg.gain.exponentialRampToValueAtTime(0.0005, t + 0.1);
      ds.connect(df).connect(dg).connect(master);
      ds.start(t);
      ds.stop(t + 0.12);
    }

    // Lonely wooden flute — slow Dorian phrase on A (A · C · D · E · D · A).
    const fluteNotes = [
      { f: 440.0, at: 0.35, d: 0.55 },
      { f: 523.25, at: 0.95, d: 0.45 },
      { f: 587.33, at: 1.45, d: 0.5 },
      { f: 659.25, at: 2.0, d: 0.6 },
      { f: 587.33, at: 2.65, d: 0.4 },
      { f: 440.0, at: 3.05, d: 0.3 },
    ];
    fluteNotes.forEach((n) => {
      const t = now + n.at;

      const osc = c.createOscillator();
      osc.type = "triangle";
      osc.frequency.setValueAtTime(n.f, t);

      // Gentle vibrato so the wooden flute feels breathed.
      const lfo = c.createOscillator();
      lfo.type = "sine";
      lfo.frequency.value = 5.2;
      const lfoGain = c.createGain();
      lfoGain.gain.value = 2.2;
      lfo.connect(lfoGain).connect(osc.frequency);

      const og = c.createGain();
      og.gain.setValueAtTime(0.0001, t);
      og.gain.linearRampToValueAtTime(0.085, t + 0.08);
      og.gain.setValueAtTime(0.085, t + n.d * 0.65);
      og.gain.exponentialRampToValueAtTime(0.0005, t + n.d);
      osc.connect(og).connect(master);
      osc.start(t);
      osc.stop(t + n.d + 0.05);
      lfo.start(t);
      lfo.stop(t + n.d + 0.05);

      // Breathy air noise on the attack to sell the wooden pipe.
      const breath = c.createBufferSource();
      breath.buffer = noiseBuffer(Math.min(0.5, n.d + 0.1));
      const bbp = c.createBiquadFilter();
      bbp.type = "bandpass";
      bbp.frequency.value = n.f * 2;
      bbp.Q.value = 6;
      const bg = c.createGain();
      bg.gain.setValueAtTime(0.0001, t);
      bg.gain.linearRampToValueAtTime(0.024, t + 0.05);
      bg.gain.exponentialRampToValueAtTime(0.0005, t + n.d * 0.9);
      breath.connect(bbp).connect(bg).connect(master);
      breath.start(t);
      breath.stop(t + n.d + 0.05);
    });
  }

  function playWestStates() {
    const c = ensure();
    if (!c) return;
    const now = c.currentTime;
    const dur = 3.6;

    // Hollow wind keening through broken battlements.
    const wind = c.createBufferSource();
    wind.buffer = noiseBuffer(dur);
    const windLp = c.createBiquadFilter();
    windLp.type = "lowpass";
    windLp.frequency.setValueAtTime(240, now);
    windLp.frequency.linearRampToValueAtTime(780, now + 1.4);
    windLp.frequency.linearRampToValueAtTime(210, now + dur);
    windLp.Q.value = 5;
    const windG = c.createGain();
    windG.gain.setValueAtTime(0.0001, now);
    windG.gain.linearRampToValueAtTime(0.38, now + 0.8);
    windG.gain.linearRampToValueAtTime(0.22, now + 2.3);
    windG.gain.exponentialRampToValueAtTime(0.0005, now + dur);
    wind.connect(windLp).connect(windG).connect(master);
    wind.start(now);
    wind.stop(now + dur);

    // Low rumble of an abandoned plain — sub-bass drone.
    const rumble = c.createOscillator();
    rumble.type = "sine";
    rumble.frequency.setValueAtTime(46, now);
    rumble.frequency.linearRampToValueAtTime(40, now + dur);
    const rumbleG = c.createGain();
    rumbleG.gain.setValueAtTime(0.0001, now);
    rumbleG.gain.linearRampToValueAtTime(0.12, now + 0.9);
    rumbleG.gain.linearRampToValueAtTime(0.07, now + dur - 0.6);
    rumbleG.gain.exponentialRampToValueAtTime(0.0005, now + dur);
    rumble.connect(rumbleG).connect(master);
    rumble.start(now);
    rumble.stop(now + dur + 0.05);

    // Mournful horn call — descending minor third G3 → E3, war long forgotten.
    const hornNotes = [
      { f: 196.0, at: 0.4, d: 0.95 },
      { f: 164.81, at: 1.45, d: 1.35 },
    ];
    hornNotes.forEach((n) => {
      const t = now + n.at;

      const horn = c.createOscillator();
      horn.type = "sawtooth";
      horn.frequency.setValueAtTime(n.f, t);
      const hornLp = c.createBiquadFilter();
      hornLp.type = "lowpass";
      hornLp.frequency.value = n.f * 4.5;
      const hg = c.createGain();
      hg.gain.setValueAtTime(0.0001, t);
      hg.gain.linearRampToValueAtTime(0.08, t + 0.25);
      hg.gain.setValueAtTime(0.08, t + n.d * 0.55);
      hg.gain.exponentialRampToValueAtTime(0.0005, t + n.d);
      horn.connect(hornLp).connect(hg).connect(master);
      horn.start(t);
      horn.stop(t + n.d + 0.05);

      // Air through the bell of the horn.
      const breath = c.createBufferSource();
      breath.buffer = noiseBuffer(n.d + 0.1);
      const bbp = c.createBiquadFilter();
      bbp.type = "bandpass";
      bbp.frequency.value = n.f * 3;
      bbp.Q.value = 3.5;
      const bg = c.createGain();
      bg.gain.setValueAtTime(0.0001, t);
      bg.gain.linearRampToValueAtTime(0.03, t + 0.15);
      bg.gain.exponentialRampToValueAtTime(0.0005, t + n.d);
      breath.connect(bbp).connect(bg).connect(master);
      breath.start(t);
      breath.stop(t + n.d + 0.05);
    });

    // Distant creaks — old timber and hinges groaning.
    [1.05, 2.25, 2.95].forEach((at) => {
      const t = now + at;
      const cs = c.createBufferSource();
      cs.buffer = noiseBuffer(0.4);
      const cf = c.createBiquadFilter();
      cf.type = "bandpass";
      cf.frequency.setValueAtTime(360 + Math.random() * 180, t);
      cf.frequency.linearRampToValueAtTime(220, t + 0.3);
      cf.Q.value = 11;
      const cg = c.createGain();
      cg.gain.setValueAtTime(0.0001, t);
      cg.gain.linearRampToValueAtTime(0.065, t + 0.09);
      cg.gain.linearRampToValueAtTime(0.035, t + 0.22);
      cg.gain.exponentialRampToValueAtTime(0.0005, t + 0.38);
      cs.connect(cf).connect(cg).connect(master);
      cs.start(t);
      cs.stop(t + 0.42);
    });
  }

  function playSevas() {
    const c = ensure();
    if (!c) return;
    const now = c.currentTime;
    const dur = 3.3;

    // Deep bronze-bell toll: warm fundamental + bell-like partials.
    const bellFundamental = 146.83; // D3 — solemn, medieval
    const bellPartials = [1, 1.5, 2, 2.67, 4];
    const bellGains = [0.22, 0.14, 0.1, 0.06, 0.035];
    bellPartials.forEach((ratio, i) => {
      const o = c.createOscillator();
      o.type = "sine";
      o.frequency.value = bellFundamental * ratio;
      const og = c.createGain();
      og.gain.setValueAtTime(0.0001, now);
      og.gain.linearRampToValueAtTime(bellGains[i], now + 0.06);
      og.gain.exponentialRampToValueAtTime(0.0005, now + dur);
      o.connect(og).connect(master);
      o.start(now);
      o.stop(now + dur + 0.05);
    });

    // Soft water lapping in a stone basin — warm, filtered, fades in and out.
    const src = c.createBufferSource();
    src.buffer = noiseBuffer(dur);
    const lp = c.createBiquadFilter();
    lp.type = "lowpass";
    lp.frequency.setValueAtTime(480, now);
    lp.frequency.linearRampToValueAtTime(820, now + dur * 0.55);
    lp.frequency.linearRampToValueAtTime(320, now + dur);
    lp.Q.value = 1.2;
    const ng = c.createGain();
    ng.gain.setValueAtTime(0.0001, now);
    ng.gain.linearRampToValueAtTime(0.11, now + 0.6);
    ng.gain.linearRampToValueAtTime(0.07, now + dur - 0.8);
    ng.gain.exponentialRampToValueAtTime(0.0005, now + dur);
    src.connect(lp).connect(ng).connect(master);
    src.start(now);
    src.stop(now + dur);

    // Solitary water drops resonating in the hall.
    const dropTimes = [0.55, 1.2, 1.9, 2.55];
    dropTimes.forEach((t) => {
      const dt = now + t;
      const o = c.createOscillator();
      o.type = "sine";
      o.frequency.setValueAtTime(380, dt);
      o.frequency.exponentialRampToValueAtTime(165, dt + 0.3);
      const og = c.createGain();
      og.gain.setValueAtTime(0.0001, dt);
      og.gain.linearRampToValueAtTime(0.12, dt + 0.012);
      og.gain.exponentialRampToValueAtTime(0.0005, dt + 0.35);
      o.connect(og).connect(master);
      o.start(dt);
      o.stop(dt + 0.4);
    });
  }

  function playNales() {
    const c = ensure();
    if (!c) return;
    const now = c.currentTime;
    const dur = 3.5;

    // Low open-fifth drone — D1 + A1 — a Saracen tanpura-style underlay.
    [
      { f: 73.42, gain: 0.11 },
      { f: 110.0, gain: 0.07 },
    ].forEach((d) => {
      const osc = c.createOscillator();
      osc.type = "sawtooth";
      osc.frequency.value = d.f;
      const lp = c.createBiquadFilter();
      lp.type = "lowpass";
      lp.frequency.setValueAtTime(420, now);
      lp.frequency.linearRampToValueAtTime(680, now + dur * 0.55);
      lp.frequency.linearRampToValueAtTime(340, now + dur);
      lp.Q.value = 4.5;
      const og = c.createGain();
      og.gain.setValueAtTime(0.0001, now);
      og.gain.linearRampToValueAtTime(d.gain, now + 0.55);
      og.gain.linearRampToValueAtTime(d.gain * 0.6, now + dur - 0.7);
      og.gain.exponentialRampToValueAtTime(0.0005, now + dur);
      osc.connect(lp).connect(og).connect(master);
      osc.start(now);
      osc.stop(now + dur + 0.05);
    });

    // Dry sand drifting over dunes — continuous filtered whisper, no percussive grains.
    const sand = c.createBufferSource();
    sand.buffer = noiseBuffer(dur);
    const sandBp = c.createBiquadFilter();
    sandBp.type = "bandpass";
    sandBp.frequency.setValueAtTime(1900, now);
    sandBp.frequency.linearRampToValueAtTime(850, now + dur);
    sandBp.Q.value = 0.9;
    const sandG = c.createGain();
    sandG.gain.setValueAtTime(0.0001, now);
    sandG.gain.linearRampToValueAtTime(0.13, now + 0.6);
    sandG.gain.linearRampToValueAtTime(0.09, now + dur - 0.9);
    sandG.gain.exponentialRampToValueAtTime(0.0005, now + dur);
    sand.connect(sandBp).connect(sandG).connect(master);
    sand.start(now);
    sand.stop(now + dur);

    // Plucked oud-like phrase in Hijaz on D (D · Eb · F# · G · F# · D) — exotic, Middle-Eastern.
    const pluckNotes = [
      { f: 293.66, at: 0.45 },
      { f: 311.13, at: 1.0 },
      { f: 369.99, at: 1.55 },
      { f: 392.0, at: 2.1 },
      { f: 369.99, at: 2.65 },
      { f: 293.66, at: 3.1 },
    ];
    pluckNotes.forEach((n) => {
      const t = now + n.at;

      const body = c.createOscillator();
      body.type = "triangle";
      body.frequency.setValueAtTime(n.f, t);
      const bodyHp = c.createBiquadFilter();
      bodyHp.type = "highpass";
      bodyHp.frequency.value = 180;
      const bodyG = c.createGain();
      bodyG.gain.setValueAtTime(0.0001, t);
      bodyG.gain.linearRampToValueAtTime(0.1, t + 0.008);
      bodyG.gain.exponentialRampToValueAtTime(0.0005, t + 0.55);
      body.connect(bodyHp).connect(bodyG).connect(master);
      body.start(t);
      body.stop(t + 0.6);

      // Bright harmonic for the pluck's "bite".
      const bite = c.createOscillator();
      bite.type = "sine";
      bite.frequency.setValueAtTime(n.f * 2, t);
      const biteG = c.createGain();
      biteG.gain.setValueAtTime(0.0001, t);
      biteG.gain.linearRampToValueAtTime(0.045, t + 0.005);
      biteG.gain.exponentialRampToValueAtTime(0.0005, t + 0.35);
      bite.connect(biteG).connect(master);
      bite.start(t);
      bite.stop(t + 0.4);
    });

    // Distant frame drum — slow caravan pulse.
    [0.3, 1.45, 2.55].forEach((at) => {
      const t = now + at;
      const drum = c.createOscillator();
      drum.type = "sine";
      drum.frequency.setValueAtTime(96, t);
      drum.frequency.exponentialRampToValueAtTime(54, t + 0.22);
      const dg = c.createGain();
      dg.gain.setValueAtTime(0.0001, t);
      dg.gain.linearRampToValueAtTime(0.15, t + 0.006);
      dg.gain.exponentialRampToValueAtTime(0.0005, t + 0.3);
      drum.connect(dg).connect(master);
      drum.start(t);
      drum.stop(t + 0.34);
    });
  }

  const REGION_SOUNDS = {
    fpedvesga: playFrost,
    tef: playTef,
    west_states: playWestStates,
    sevas: playSevas,
    nales: playNales,
  };

  function playRegion(regionId) {
    const fn = REGION_SOUNDS[regionId];
    if (fn) fn();
  }

  return {
    unlock,
    playWhoosh,
    playClick,
    playRegion,
    playDecayingSonicBoom,
    playStressTransition,
    playSlowSwing,
    preload: preloadAudio,
  };
})();

/* ---------- English titles for overlay ---------- */

const REGION_TITLES_EN = {
  fpedvesga: "Lidenvar",
  sevas: "Sevas",
  nales: "Nales",
  tef: "Kingdom of Tef",
  west_states: "The Western Realms",
};

/* ---------- Intro sequence ---------- */

const INTRO_STEPS = [
  { id: "made-by",  enterAt: 400,   leaveAt: null  },
  { id: "author",   enterAt: 400,   leaveAt: null  },
  { id: "book",     enterAt: 8700,  leaveAt: 14100 },
  { id: "subtitle", enterAt: 12600, leaveAt: 14100 },
];

const INTRO_STEP_SOUNDS = {
  "made-by": () => AudioFX.playDecayingSonicBoom(),
  "book": () => AudioFX.playStressTransition(),
  "subtitle": () => AudioFX.playSlowSwing(),
};

const INTRO_HINT_AT = 18000;
const INTRO_AUTO_END = 14500;

function runIntro() {
  const intro = document.getElementById("intro");
  const skipBtn = document.getElementById("intro-skip");
  const enterHint = document.getElementById("intro-enter");
  const atlas = document.getElementById("atlas");
  if (!intro || !atlas) return;

  const steps = new Map();
  INTRO_STEPS.forEach((cfg) => {
    const el = intro.querySelector(`[data-step="${cfg.id}"]`);
    if (el) steps.set(cfg.id, el);
  });

  const timers = [];
  let finished = false;
  let started = false;

  enterHint?.classList.add("is-visible");

  function finishIntro() {
    if (finished) return;
    finished = true;
    timers.forEach((t) => clearTimeout(t));
    intro.classList.add("is-hidden");
    atlas.classList.add("is-visible");
    atlas.setAttribute("aria-hidden", "false");
    intro.setAttribute("aria-hidden", "true");
    document.body.classList.remove("is-intro");
    window.setTimeout(() => intro.remove(), 1200);
  }

  function startIntro() {
    if (started) return;
    started = true;
    AudioFX.unlock();
    enterHint?.classList.remove("is-visible");

    INTRO_STEPS.forEach((cfg) => {
      const el = steps.get(cfg.id);
      if (!el) return;
      timers.push(
        window.setTimeout(() => {
          el.classList.add("is-visible");
          const soundFn = INTRO_STEP_SOUNDS[cfg.id];
          if (soundFn) soundFn();
        }, cfg.enterAt)
      );
      if (cfg.leaveAt != null) {
        timers.push(
          window.setTimeout(() => el.classList.add("is-leaving"), cfg.leaveAt)
        );
      }
    });

    const autoTimer = window.setTimeout(finishIntro, INTRO_AUTO_END);
    timers.push(autoTimer);
  }

  intro.addEventListener("click", (e) => {
    if (e.target === skipBtn) return;
    if (!started) {
      startIntro();
      return;
    }
    finishIntro();
  });
  skipBtn?.addEventListener("click", (e) => {
    e.stopPropagation();
    finishIntro();
  });
  window.addEventListener("keydown", function keyHandler(e) {
    if (finished) {
      window.removeEventListener("keydown", keyHandler);
      return;
    }
    const isTrigger =
      e.key === "Enter" ||
      e.key === " " ||
      e.key === "Escape" ||
      e.key.length === 1;
    if (!isTrigger) return;
    if (!started) {
      startIntro();
      return;
    }
    finishIntro();
  });
}

/* ---------- Region fullscreen overlay ---------- */

function initOverlay() {
  const overlay = document.getElementById("region-overlay");
  const titleEl = document.getElementById("overlay-title");
  const eyebrowEl = document.getElementById("overlay-eyebrow");
  const closeBtn = document.getElementById("overlay-close");
  if (!overlay) return { open: () => {}, close: () => {} };

  function open(regionId) {
    const meta = REGIONS.find((r) => r.id === regionId);
    if (!meta) return;
    const englishTitle = REGION_TITLES_EN[regionId] || meta.title;
    if (titleEl) titleEl.textContent = englishTitle;
    if (eyebrowEl) {
      eyebrowEl.textContent =
        regionId === "west_states" ? "The Lands of" : "The Realm of";
    }
    overlay.classList.add("is-open");
    overlay.setAttribute("aria-hidden", "false");
    document.body.style.overflow = "hidden";
  }

  function close() {
    overlay.classList.remove("is-open");
    overlay.setAttribute("aria-hidden", "true");
    document.body.style.overflow = "";
  }

  closeBtn?.addEventListener("click", close);
  overlay.addEventListener("click", (e) => {
    if (e.target === overlay) close();
  });
  window.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && overlay.classList.contains("is-open")) close();
  });

  return { open, close };
}

/* ---------- App setup ---------- */

function setup() {
  const svg = document.getElementById("hit-layer");
  const titleEl = document.getElementById("panel-title");
  const bodyEl = document.getElementById("panel-body");
  const hintEl = document.getElementById("panel-hint");

  renderRegions(svg);
  const regions = svg.querySelectorAll(".region");
  const overlay = initOverlay();

  function selectRegion(poly) {
    regions.forEach((p) => p.classList.remove("is-active"));
    if (poly) poly.classList.add("is-active");
  }

  function updateSidePanel(regionId) {
    const meta = REGIONS.find((x) => x.id === regionId);
    if (!meta) return;
    const englishTitle = REGION_TITLES_EN[regionId] || meta.title;
    if (titleEl) titleEl.textContent = englishTitle;
    if (bodyEl) bodyEl.textContent = randomBody();
    if (hintEl) hintEl.textContent = "";
  }

  regions.forEach((poly) => {
    poly.addEventListener("click", (e) => {
      if (document.body.classList.contains("editor-on")) {
        e.preventDefault();
        return;
      }
      const id = poly.getAttribute("data-id");
      selectRegion(poly);
      updateSidePanel(id);
      AudioFX.playRegion(id);
      overlay.open(id);
    });
    poly.addEventListener("keydown", (e) => {
      if (document.body.classList.contains("editor-on")) return;
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        poly.click();
      }
    });
  });

  initEditor(svg, regions);
  initGlobalClickSound();
  AudioFX.unlock();
  AudioFX.preload();
  runIntro();
}

function initGlobalClickSound() {
  window.addEventListener(
    "pointerdown",
    (e) => {
      if (document.body.classList.contains("editor-on")) return;
      const t = e.target;
      if (t && t.closest && t.closest(".region")) return;
      AudioFX.playClick();
    },
    { capture: true }
  );
}

document.addEventListener("DOMContentLoaded", setup);
